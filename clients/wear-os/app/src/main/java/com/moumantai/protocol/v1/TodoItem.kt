// Code generated by Wire protocol buffer compiler, do not edit.
// Source: moumantai.v1.TodoItem in moumantai/v1/chat.proto
@file:Suppress(
  "DEPRECATION",
  "RUNTIME_ANNOTATION_NOT_SUPPORTED",
)

package com.moumantai.protocol.v1

import android.os.Parcelable
import com.squareup.wire.AndroidMessage
import com.squareup.wire.FieldEncoding
import com.squareup.wire.ProtoAdapter
import com.squareup.wire.ProtoReader
import com.squareup.wire.ProtoWriter
import com.squareup.wire.ReverseProtoWriter
import com.squareup.wire.Syntax.PROTO_3
import com.squareup.wire.WireField
import com.squareup.wire.`internal`.JvmField
import com.squareup.wire.`internal`.sanitize
import kotlin.Any
import kotlin.AssertionError
import kotlin.Boolean
import kotlin.Deprecated
import kotlin.DeprecationLevel
import kotlin.Int
import kotlin.Long
import kotlin.Nothing
import kotlin.String
import kotlin.Suppress
import okio.ByteString

/**
 * One item in the edit-agent's TodoWrite list.
 */
public class TodoItem(
  /**
   * Task description (declarative, e.g. "Implement user authentication").
   */
  @field:WireField(
    tag = 1,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 0,
  )
  public val content: String = "",
  /**
   * Current status.
   */
  @field:WireField(
    tag = 2,
    adapter = "com.moumantai.protocol.v1.TodoStatus#ADAPTER",
    label = WireField.Label.OMIT_IDENTITY,
    schemaIndex = 1,
  )
  public val status: TodoStatus = TodoStatus.TODO_STATUS_UNSPECIFIED,
  /**
   * Gerund form shown while in progress (e.g. "Implementing user
   * authentication"). PWA shows `active_form` when status == IN_PROGRESS,
   * else `content`.
   */
  @field:WireField(
    tag = 3,
    adapter = "com.squareup.wire.ProtoAdapter#STRING",
    label = WireField.Label.OMIT_IDENTITY,
    jsonName = "activeForm",
    schemaIndex = 2,
  )
  public val active_form: String = "",
  unknownFields: ByteString = ByteString.EMPTY,
) : AndroidMessage<TodoItem, Nothing>(ADAPTER, unknownFields) {
  @Deprecated(
    message = "Shouldn't be used in Kotlin",
    level = DeprecationLevel.HIDDEN,
  )
  override fun newBuilder(): Nothing = throw
      AssertionError("Builders are deprecated and only available in a javaInterop build; see https://square.github.io/wire/wire_compiler/#kotlin")

  override fun equals(other: Any?): Boolean {
    if (other === this) return true
    if (other !is TodoItem) return false
    if (unknownFields != other.unknownFields) return false
    if (content != other.content) return false
    if (status != other.status) return false
    if (active_form != other.active_form) return false
    return true
  }

  override fun hashCode(): Int {
    var result = super.hashCode
    if (result == 0) {
      result = unknownFields.hashCode()
      result = result * 37 + content.hashCode()
      result = result * 37 + status.hashCode()
      result = result * 37 + active_form.hashCode()
      super.hashCode = result
    }
    return result
  }

  override fun toString(): String {
    val result = mutableListOf<String>()
    result += """content=${sanitize(content)}"""
    result += """status=$status"""
    result += """active_form=${sanitize(active_form)}"""
    return result.joinToString(prefix = "TodoItem{", separator = ", ", postfix = "}")
  }

  public fun copy(
    content: String = this.content,
    status: TodoStatus = this.status,
    active_form: String = this.active_form,
    unknownFields: ByteString = this.unknownFields,
  ): TodoItem = TodoItem(content, status, active_form, unknownFields)

  public companion object {
    @JvmField
    public val ADAPTER: ProtoAdapter<TodoItem> = object : ProtoAdapter<TodoItem>(
      FieldEncoding.LENGTH_DELIMITED, 
      TodoItem::class, 
      "type.googleapis.com/moumantai.v1.TodoItem", 
      PROTO_3, 
      null, 
      "moumantai/v1/chat.proto"
    ) {
      override fun encodedSize(`value`: TodoItem): Int {
        var size = value.unknownFields.size
        if (value.content != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(1, value.content)
        }
        if (value.status != com.moumantai.protocol.v1.TodoStatus.TODO_STATUS_UNSPECIFIED) {
          size += TodoStatus.ADAPTER.encodedSizeWithTag(2, value.status)
        }
        if (value.active_form != "") {
          size += ProtoAdapter.STRING.encodedSizeWithTag(3, value.active_form)
        }
        return size
      }

      override fun encode(writer: ProtoWriter, `value`: TodoItem) {
        if (value.content != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.content)
        }
        if (value.status != com.moumantai.protocol.v1.TodoStatus.TODO_STATUS_UNSPECIFIED) {
          TodoStatus.ADAPTER.encodeWithTag(writer, 2, value.status)
        }
        if (value.active_form != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 3, value.active_form)
        }
        writer.writeBytes(value.unknownFields)
      }

      override fun encode(writer: ReverseProtoWriter, `value`: TodoItem) {
        writer.writeBytes(value.unknownFields)
        if (value.active_form != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 3, value.active_form)
        }
        if (value.status != com.moumantai.protocol.v1.TodoStatus.TODO_STATUS_UNSPECIFIED) {
          TodoStatus.ADAPTER.encodeWithTag(writer, 2, value.status)
        }
        if (value.content != "") {
          ProtoAdapter.STRING.encodeWithTag(writer, 1, value.content)
        }
      }

      override fun decode(reader: ProtoReader): TodoItem {
        var content: String = ""
        var status: TodoStatus = TodoStatus.TODO_STATUS_UNSPECIFIED
        var active_form: String = ""
        val unknownFields = reader.forEachTag { tag ->
          when (tag) {
            1 -> content = ProtoAdapter.STRING.decode(reader)
            2 -> try {
              status = TodoStatus.ADAPTER.decode(reader)
            } catch (e: ProtoAdapter.EnumConstantNotFoundException) {
              reader.addUnknownField(tag, FieldEncoding.VARINT, e.value.toLong())
            }
            3 -> active_form = ProtoAdapter.STRING.decode(reader)
            else -> reader.readUnknownField(tag)
          }
        }
        return TodoItem(
          content = content,
          status = status,
          active_form = active_form,
          unknownFields = unknownFields
        )
      }

      override fun redact(`value`: TodoItem): TodoItem = value.copy(
        unknownFields = ByteString.EMPTY
      )
    }

    @JvmField
    public val CREATOR: Parcelable.Creator<TodoItem> = AndroidMessage.newCreator(ADAPTER)

    private const val serialVersionUID: Long = 0L
  }
}
