# Dev defaults — Vite auto-loads this in `npm run dev` mode.
# Production builds (`npm run build`) ignore it.
#
# The PWA's Vite dev server runs on :5174 but the Moumantai backend lives on
# :3000. Without this, the WebSocket would point at :5174 and fail.
VITE_WS_URL=ws://localhost:3000
