*.iml
.gradle
/local.properties
.DS_Store
**/build/
/captures
*.stackdump
*_crash.log
.externalNativeBuild
.cxx
local.properties
.playwright-mcp
.claude/worktrees/
.claude/scheduled_tasks.lock

# Ad-hoc debug screenshots at repo root (keep intentional assets in docs/)
/*.png

# Python
__pycache__/
*.pyc
.venv/

# Android build artifacts
*.apk

# Node
node_modules/

# Environment secrets
.env
.env.local

# mise per-machine config (template: .mise.local.toml.example)
.mise.local.toml

# Build artifacts
.vite/
*.tsbuildinfo
dist/

# Server runtime data (per-app SQLite databases, conversation store, etc.)
server/data/

# Project-local Moumantai workspace override (preferred over ~/.moumantai/ when present)
.moumantai/

# Local Claude Code settings (per-machine permissions + paths)
.claude/settings.local.json

# Internal working notes (release audit, etc.) — never shipped publicly
/.notes/

# Local databases
*.sqlite
*.sqlite-journal
*.db-journal

# IDE / editor
.idea/
.vscode/
*.swp
*~

# Cross-language fixture round-trip artifacts (regenerated by
# `task protocol:test-cross-language`; only the .json source is checked in).
shared/protocol/fixtures/**/*.ts.bin
shared/protocol/fixtures/**/*.kotlin.bin
shared/protocol/fixtures/**/*.c.bin
