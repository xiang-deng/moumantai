import type { AppManifest } from 'moumantai'

export const manifest: AppManifest = {
  id: '__APP_ID__',
  version: '0.1.0',
  name: '__APP_NAME__',
  icon: '__ICON__',
  // Optional M3 brand seed (hex), e.g. '#00897B'. Omit to use the default palette.
  // color: '#00897B',
  // Double-quoted so descriptions containing apostrophes remain valid TS.
  description: "__APP_DESC__",
}
