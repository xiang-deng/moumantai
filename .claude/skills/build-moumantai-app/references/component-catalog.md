# Component catalog (generated)

> Generated by `scripts/sync-catalog.ts` from `server/src/server/protocol/catalog.ts`.
> Do NOT edit by hand — changes will be overwritten. Update `catalog.ts` and rerun:
>
> ```bash
> npx tsx .claude/skills/build-moumantai-app/scripts/sync-catalog.ts
> ```

All builders are imported from `moumantai/ui`. Use `pathRef` for data binding (see `faces.md` for the two forms).

Compact-first rule: the default face variant (no suffix) must render on watch (192dp) and iot-small (240dp). Watch auto-skips `TopBar` and wraps `Row>2` to `Column`.

## Atoms

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `text` | `text(id, content, {typography})` | phone, watch, iot-small, hmi-panel, glass | Display text with typography styling |
| `icon` | `icon(id, name)` | phone, watch, iot-small, hmi-panel, glass | Material Symbols by name (e.g. "check_circle"); prefix with "fa:" for FontAwesome Free fallback (e.g. "fa:cart-plus") |
| `image` | `image(id, src)` | phone, watch, iot-small, hmi-panel | Image from URL |
| `divider` | `divider(id)` | phone, watch, iot-small, hmi-panel | Visual separator line |

## Layout

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `column` | `column(id, [children], {spacing, padding})` | phone, watch, iot-small, hmi-panel, glass | Vertical flex container |
| `row` | `row(id, [children], {spacing})` | phone, watch, iot-small, hmi-panel | Horizontal flex container |
| `card` | `card(id, [children])` | phone, watch, iot-small, hmi-panel | Elevated/outlined container |

**Small-screen notes:**
- `column` — Watch: auto safe-area inset (28dp) on round screen
- `row` — Watch: wraps to Column if >2 children
- `card` — Watch: radius 8dp, padding 4-8dp (compact)

## Chrome

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `scaffold` | `scaffold(id, {body, top_bar})` | phone, watch, iot-small, hmi-panel | Top-level screen container with top bar, body, and FAB slots |
| `topBar` | `topBar(id, title)` | phone, watch, iot-small, hmi-panel | App bar with title and navigation |

**Small-screen notes:**
- `scaffold` — Watch: renders body slot only (TopBar and FAB skipped)
- `topBar` — Watch: skipped entirely to save screen space

## Actions

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `button` | `button(id, label, {variant, icon, action})` | phone, watch, iot-small, hmi-panel | Clickable button (variants: filled, tonal, outlined, text, fab) |
| `chip` | `chip(id, label, {selected, action})` | phone, watch, iot-small, hmi-panel | Filter or assist chip with selection state |

**Small-screen notes:**
- `button` — Watch: icon-only when both icon and text present; 48dp min touch target
- `chip` — Watch/iot-small: renders full-width; primary interactive element on watch

## Input

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `textField` | `textField(id, value, label)` | phone, watch, iot-small, hmi-panel | Text input field |
| `checkBox` | `checkBox(id, label, checked)` | phone, watch, iot-small, hmi-panel | Boolean checkbox toggle |
| `switchToggle` | `switchToggle(id, checked)` | phone, watch, iot-small, hmi-panel | On/off toggle switch |
| `slider` | `slider(id, value, {min, max})` | phone, watch, iot-small, hmi-panel | Numeric range slider |
| `tabs` | `tabs(id, labels, content)` | phone, watch, iot-small, hmi-panel | Tabbed content switcher |
| `select` | `select(id, value, label, {options})` | phone, watch, iot-small, hmi-panel | Pick one option from a dropdown list |
| `dateTimeInput` | `dateTimeInput(id, value, label, {mode})` | phone, watch, iot-small, hmi-panel | Date and/or time picker |

**Small-screen notes:**
- `textField` — Watch/iot-small: opens full-screen input overlay (voice-first)
- `tabs` — Watch/iot-small: shows active tab content only, no tab bar
- `select` — Watch/iot-small: full-screen picker overlay
- `dateTimeInput` — Watch/iot-small: full-screen picker overlay

## Data

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `list` | `list(id, itemsPath, templateId)` | phone, watch, iot-small, hmi-panel | Scrollable data-driven list with template binding |
| `listItem` | `listItem(id, headline, {supporting, trailing_content})` | phone, watch, iot-small, hmi-panel | List item with headline, supporting text, and trailing content |

**Small-screen notes:**
- `list` — Watch: 44dp item height; iot-small: 48dp; hmi-panel: 52dp
- `listItem` — Watch: 44dp item height; iot-small: 48dp; hmi-panel: 52dp

## Feedback

| Builder | Signature | Devices | Description |
|---|---|---|---|
| `progress` | `progress(id, value, max, {variant})` | phone, watch, iot-small, hmi-panel | Progress indicator (variants: circular, linear) |
| `modal` | `modal(id, [children], {open})` | phone, watch, iot-small, hmi-panel | Overlay dialog |

**Small-screen notes:**
- `progress` — Watch: circular variant only
- `modal` — Watch/iot-small: renders full-screen edge-to-edge

---

Total components: 23. Builders are always called with an `id: string` as the first argument.
